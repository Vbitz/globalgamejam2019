{
  "name": "game1",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "gts": "^0.9.0",
    "typescript": "~3.1.0",
    "@types/three": "^0.93.18",
    "parcel-bundler": "^1.11.0"
  },
  "dependencies": {
    "three": "^0.100.0"
  },
  "scripts": {
    "check": "gts check",
    "clean": "gts clean",
    "compile": "tsc -p .",
    "fix": "gts fix",
    "start:dev": "parcel --no-hmr src/index.html",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run check"
  }
}